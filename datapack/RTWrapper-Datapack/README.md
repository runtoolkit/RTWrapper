# RTWrapper Datapack

Standalone datapack build of RTWrapper. Drop this folder/zip into a world's `datapacks/` directory, then run `/reload`.

The Fabric mod embeds the same `data/` tree, so mod users do not need to install this datapack separately.
